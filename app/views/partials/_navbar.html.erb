<nav>
  <div class="nav-container">
    <div class="left-nav">
      <div class="logo">
        <%= link_to '<i class="fa-solid fa-paw"></i> <span>PetCare</span>'.html_safe, '/' %>
      </div>
    </div>

    <div class="right-nav">
      <%= link_to 'Hazte anfitrión', '/' %>
      <ul class="menus" id="menus">
        <li onclick="showGeneralMenu()" class="filter-options" id="filter-options">
          <i class="fa-solid fa-bars"><%= render 'partials/general_options' %></i>
        </li>
        <li onclick="showUserMenu()" class="user-options" id="user-options">
          <i class="fa-regular fa-user"><%= render 'partials/sessions_options' %></i>
        </li>
      </ul>
      <div class="notification-menu" onclick="showNotifications()">
        <i class="fa-solid fa-bell">
          <% unless current_user.host_reservations.where(status: 0).size.zero? %>
            <div class="reservation-count">
              <span><%= current_user.host_reservations.where(status: 0).size %></span>
            </div>
            <%= render "partials/notifications" %>
          <% end %>
        </i>
      </div>
    </div>
  </div>
</nav>
