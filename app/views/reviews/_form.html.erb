<%= form_with(model: review, class: 'review-form') do |form| %>
  <% if review.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
        <% review.errors.each do |error| %>
          <li><%= error.attribute %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="hidden">
    <%= form.hidden_field :home_id, value: @home.id %>
  </div>

  <div class="hidden">
    <%= form.hidden_field :guest_id, value: current_user.id %>
  </div>
  
  <div class="field">
    <%= form.label :score, 'Puntuación', style: "display: block" %>
    <%= form.range_field :score, in: 0..5, step: 0.1, class: 'slider', id: 'my-range' %>
  </div>

  <div class="field">
    <%= form.label :comment, 'Comentario', style: "display: block" %>
    <%= form.text_area :comment %>
  </div>

  <div class="confirm">
    <div class="actions">
      <%= form.submit 'Guardar comentario' %>
    </div>
  </div>
<% end %>
